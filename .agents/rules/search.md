---
trigger: always_on
---

This development environment is running on Windows 11. Use select-string for search. Never use grep for search, as that is a Linux command.

Most functionality is contained in index.html. Other files within the project are in support of the local installations only. Unless you are modifying a local install only feature, start with index.html first. Do not waste time looking through other files unnecessarily.

Always reference instructions.md and the category list at the top of index.html before searching through the code. These will help you navigate without searching blindly.